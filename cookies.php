<?php
setcookie('str', 'Hello World');

var_dump($_COOKIE);


// A cookie is a small piece of data stored on the client’s browser.

// Used to remember information between page reloads or visits (e.g., username, preferences).

// Stored on the user’s machine and sent with each HTTP request.

// Less secure because users can view/modify cookies.